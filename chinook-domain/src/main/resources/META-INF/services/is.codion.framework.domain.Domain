# This old school service configuration is only
# included so that the domain is registered as a
# service when used in unit tests, which don't
# run on the module path
is.codion.demos.chinook.domain.ChinookImpl