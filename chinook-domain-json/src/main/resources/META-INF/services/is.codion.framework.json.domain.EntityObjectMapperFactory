# Included so that the object mapper factory is registered on Android,
# which has no module system and therefore ignores the module-info provides clause
is.codion.demos.chinook.domain.json.ChinookObjectMapperFactory