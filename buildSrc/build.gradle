plugins {
    id 'groovy-gradle-plugin'
}

repositories {
    gradlePluginPortal()
}

dependencies {
    implementation "org.beryx:badass-jlink-plugin:${jlinkVersion}"
    implementation "org.gradlex:extra-java-module-info:${extraJavaModuleInfoVersion}"
}